<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sniper.game.wordgame.mapper.UserFeedbackMapper">

    <insert id="insert" parameterType="com.sniper.game.wordgame.entity.UserFeedback"
            useGeneratedKeys="true" keyProperty="id">
        insert into user_feedback (
            user_id, game_type, feedback_type, content, source, created_at, updated_at
        ) values (
            #{userId},
            #{gameType, jdbcType=INTEGER, typeHandler=com.sniper.game.wordgame.typehandler.GameTypeEnumTypeHandler},
            #{feedbackType, jdbcType=INTEGER, typeHandler=com.sniper.game.wordgame.typehandler.FeedbackTypeEnumTypeHandler},
            #{content},
            #{source, jdbcType=INTEGER, typeHandler=com.sniper.game.wordgame.typehandler.SourceEnumTypeHandler},
            now(),
            now()
        )
    </insert>

    <!-- 当天已提交条数：created_at >= 今天 00:00:00，用于每日限流 -->
    <select id="countTodayByUser" resultType="java.lang.Integer">
        select count(*)
        from user_feedback
        where user_id = #{userId}
          and created_at >= #{today}
    </select>
</mapper>
